spring.application.name=FinancialTransactionsMonitor

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/${DB_NAME}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Server Configuration
server.port=8080

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Logging
logging.level.com.portfolio=DEBUG
logging.level.org.springframework.web=DEBUG

#Security
jwt.token.secretKey=asdsaoodsaosodcksoacksocksaokcoskcasokcasokcmmcmmcmc
jwt.token.duration=36000000
toke.sekret=sadasdasdcxkcxzkcxkkxzckzkmcksmdkasmdkasmdasd